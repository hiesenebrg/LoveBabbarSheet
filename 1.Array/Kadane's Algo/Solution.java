import java.util.*;

class Solution {
    public static void main(String[] args) {
        Integer[] arr = { 1, 13, 14, 15 };
        // List<Integer> arrs = new ArrayList<>();
        // Collections.addAll(arrs, arr);
        List al = Arrays.asList(arr);
        System.out.println(al);
    }
}
